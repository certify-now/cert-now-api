global:
  resolve_timeout: 5m
  # Email (SMTP) — reuses your existing app mail credentials
  smtp_smarthost: 'smtp.gmail.com:587'
  smtp_from: '${MAIL_USERNAME}'
  smtp_auth_username: '${MAIL_USERNAME}'
  smtp_auth_password: '${MAIL_PASSWORD}'
  smtp_require_tls: true

route:
  receiver: 'staging-alerts'
  group_by: ['alertname', 'environment']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

  routes:
    # Prod alerts — all severities
    - matchers:
        - environment = "prod"
      receiver: 'prod-alerts'
      routes:
        - matchers:
            - severity = "critical"
          receiver: 'prod-alerts'
          repeat_interval: 1h

    # Staging alerts — all severities
    - matchers:
        - environment = "staging"
      receiver: 'staging-alerts'
      routes:
        - matchers:
            - severity = "critical"
          receiver: 'staging-alerts'
          repeat_interval: 1h

receivers:
  - name: 'staging-alerts'
    email_configs:
      - to: '${ALERT_EMAIL_TO}'
        send_resolved: true
        headers:
          subject: '[{{ .Status | toUpper }}][STAGING] {{ .GroupLabels.alertname }}'
        body: |
          {{ range .Alerts }}
          Alert: {{ .Annotations.summary }}
          Details: {{ .Annotations.description }}
          Severity: {{ .Labels.severity }}
          Environment: staging
          Started: {{ .StartsAt.Format "2006-01-02 15:04:05 UTC" }}
          {{ end }}
    slack_configs:
      - api_url: '${SLACK_WEBHOOK_URL_STAGING}'
        channel: '${SLACK_ALERT_CHANNEL_STAGING}'
        send_resolved: true
        title: '{{ if eq .Status "firing" }}🔴{{ else }}✅{{ end }} [STAGING] {{ .GroupLabels.alertname }}'
        text: |
          {{ range .Alerts }}
          *{{ .Annotations.summary }}*
          {{ .Annotations.description }}
          Severity: `{{ .Labels.severity }}`
          {{ end }}

  - name: 'prod-alerts'
    email_configs:
      - to: '${ALERT_EMAIL_TO}'
        send_resolved: true
        headers:
          subject: '[{{ .Status | toUpper }}][PROD] {{ .GroupLabels.alertname }}'
        body: |
          {{ range .Alerts }}
          Alert: {{ .Annotations.summary }}
          Details: {{ .Annotations.description }}
          Severity: {{ .Labels.severity }}
          Environment: prod
          Started: {{ .StartsAt.Format "2006-01-02 15:04:05 UTC" }}
          {{ end }}
    slack_configs:
      - api_url: '${SLACK_WEBHOOK_URL_PROD}'
        channel: '${SLACK_ALERT_CHANNEL_PROD}'
        send_resolved: true
        title: '{{ if eq .Status "firing" }}🔴{{ else }}✅{{ end }} [PROD] {{ .GroupLabels.alertname }}'
        text: |
          {{ range .Alerts }}
          *{{ .Annotations.summary }}*
          {{ .Annotations.description }}
          Severity: `{{ .Labels.severity }}`
          {{ end }}

inhibit_rules:
  - source_matchers:
      - alertname = "AppDown"
    target_matchers:
      - alertname =~ "HighHeapUsage|MetaspaceNearLimit|HighErrorRate|SlowResponseTime|HighGCOverhead"
    equal: ['environment']
